<style is:global>
    :root {
        --color-primary: white;
        --color-primary-dark: #e6e6e6;
        --color-secondary: #e08900;
        --color-secondary-light: #fa991c;

        --color-text-primary: #373737;
        --color-text-primary-light: #888888;
        --color-text-primary-lighter: #c2c2c2;

        --color-text-secondary: #784503;

        direction: rtl;
    }
    html {
        font-size: 16px; /* set the base font size */
        scroll-behavior: smooth; /* add smooth scrolling to the page */
        height: 100%;
    }

    body {
        margin: 0;
        padding: 0;
        min-height: 100%;
        position: relative;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        font-family: Times, serif; /* set the default font */
        background-color: var(--color-primary); /* set the background color */
        color: var(--color-text-primary); /* set the default text color */
    }
    *,
    *::after,
    *::before {
        box-sizing: border-box;
    }
</style>
